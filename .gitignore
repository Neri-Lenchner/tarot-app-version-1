node_modules/
dist/
build/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
notes/
teach-notes.txt

# Internal working/scratch docs — excluded so a public demo repo doesn't
# expose leftover AI-assistant task-tracking notes.
APROVED-CHANGES.md
CSS-CHANGES.md

# The exact verbatim prompt text sent to OpenAI — kept private (unlike
# AI-INTERPRETATION.md, which explains the method without handing over the
# full copy-pasteable ruleset).
PROMPT-EXAMPLE.md
